package com.reading.reading.read.csv;

import java.io.IOException;

import com.reading.reading.read.ReadingProcessFile;

public class ReadingCSVParser implements ReadingProcessFile {

	@Override
	public void read(String fileName) throws IOException {
		/*
		 * Pending to implement. 
		 * The idea is process the CSV File putting the information in Map<String, ClientDto>
		 * Then call ResultToPrint.executeResult((Map<String, ClientDto> mapClient (Implemented)) and the results will be showed on console
		 */
	}

}
